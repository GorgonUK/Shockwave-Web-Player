# Use the patched WASM bundle from `npm run dirplayer:build-local` (see README).
# Set to `upstream` to compare against the stock polyfill in public/dirplayer/dirplayer-polyfill.js
VITE_DIRPLAYER_RUNTIME_SOURCE=local
